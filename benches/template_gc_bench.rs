// Template benchmark harness for `fugrip`.
// This file is intentionally minimal — add `criterion` and flesh out microbenchmarks as needed.

#![allow(dead_code)]

use std::sync::Arc;

fn main() {
    // Bench harness placeholder — replace with `criterion` benchmark harness.
    println!("Template GC bench. Add criterion and benchmarks here.");
}
